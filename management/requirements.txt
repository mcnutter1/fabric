# Fabric Management Layer — Python dependencies.
#
# Version floors (>=) instead of exact pins so a fresh install always resolves
# to a wheel that exists for the host's Python (avoids source builds / Rust on
# new interpreters like 3.13/3.14). No virtualenv is used; these install into
# the system interpreter (see scripts/install-management.sh).
fastapi>=0.111
uvicorn[standard]>=0.30
SQLAlchemy>=2.0.30
pydantic>=2.7
pydantic-settings>=2.3
python-multipart>=0.0.9
Jinja2>=3.1.4
itsdangerous>=2.2
httpx>=0.27
cryptography>=43.0.0
websockets>=12.0
PyYAML>=6.0.2
qrcode>=7.4.2
pypng>=0.20220715.0
# Optional production extras (Postgres, Redis pub/sub, Route53, GeoIP)
psycopg[binary]>=3.1.19
redis>=5.0
boto3>=1.34
geoip2>=4.8
